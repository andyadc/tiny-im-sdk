<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.andyadc.im</groupId>
    <artifactId>tiny-im-sdk</artifactId>
    <packaging>pom</packaging>
    <version>1.0.0</version>

    <modules>
        <module>tiny-im-sdk-starter</module>
        <module>tiny-im-sdk-interfaces</module>
        <module>tiny-im-sdk-infrastructure</module>
        <module>tiny-im-sdk-domain</module>
        <module>tiny-im-sdk-application</module>
    </modules>

    <properties>
        <java.version>1.8</java.version>
        <encoding>UTF-8</encoding>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>${encoding}</project.build.sourceEncoding>
        <project.reporting.outputEncoding>${encoding}</project.reporting.outputEncoding>
        <maven.test.skip>true</maven.test.skip>
        <argLine>
            -Dfile.encoding=${encoding}
        </argLine>
    </properties>

</project>
